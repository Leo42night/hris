import './bootstrap';

// Make the function globally accessible by assigning it to the window object
// window.isJsonString = function(str) {
//   try {
//       JSON.parse(str);
//       return true; // If successful, it's a valid JSON string
//   } catch (e) {
//       return false; // If an error occurs, it's not a valid JSON string
//   }
// };
